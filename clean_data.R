print("Cleaning data")
# TBD

## potentially include the following checks: 
# 1. tokens which have experienced at least 30 transfers 
# 2. tokens which are in erc20contractstats (because then we have their name and symbol)
# 3. only include tokens which appear in more than one block (because then there is some action)